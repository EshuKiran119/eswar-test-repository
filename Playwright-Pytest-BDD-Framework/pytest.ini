[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers --strict-config
bdd_features_base_dir = features
markers =
    api: HTTP-level behavior and contracts
    ui: browser-level behavior
    smoke: small release check
    regression: negative and integration coverage
    auth: session lifecycle
    mapping: stored data and calculated values
